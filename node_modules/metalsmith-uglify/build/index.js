'use strict';

require('metalsmith')(__dirname)
  .use(require('metalsmith-watch')({
    paths: {
      '${source}/**/*': true
    },
    livereload: true
  }))
  .use(require('../')({
    concat: 'static/js/ffa.min.js',
    removeOriginal: true
  }))
  .build(function (err) {
    if (err) { throw err; }
  });
